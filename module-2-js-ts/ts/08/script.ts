// API 
type inventory = {
    name: string,
    id: string,
    category: string,
}

type employees = {
    name: string,
    department: string,
    necessaryEquipment: string    
}

function invetory(inventory: inventory) {}

function addToInvetory(inventory: inventory) {}

function removeFromInvetory(inventory: inventory) {}

function getEmployeeData(employee: employees) {}

function manageEmployees(employee: employees) {}

